// ReadingTimeApp.swift
//
// App entry point. When creating the Xcode project, name the target
// "ReadingTime" (or update this if you name it something else — the
// @main struct name doesn't need to match, but it's convenient if it does).

import SwiftUI

@main
struct ReadingTimeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
